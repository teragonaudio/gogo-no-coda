
Makefile_dll.mingw	;	for Mingw32-gcc by moto(for DLL)
Makefile.mingw		;	for Mingw32-gcc by moto(for console)

Makefile.nocygwin	;	for cygwin? by pot (for DLL/console)

Makefile.bcc		;	for Borland C++ (for Console/cf. contrib/BCCdeSSE)
Makefile_dll.bcc	;       for Borland C++ (for DLL)

Makefile.installer.mingw;       for WinGOGO Installer (for DLL)
